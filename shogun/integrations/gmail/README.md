# integrations/gmail

Status: **not implemented yet** — design only, see `docs/ARCHITECTURE.md` and `docs/IMPLEMENTATION_PLAN.md`.

Intent→MCP adapter for mail: search/read (Level 0), draft (Level 1), send (Level 2). Target milestone: MVP0.4, likely delegated through `ai/claude` rather than a direct client — see `docs/ARCHITECTURE.md` §7.
