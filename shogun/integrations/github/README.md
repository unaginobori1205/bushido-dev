# integrations/github

Status: **not implemented yet** — design only, see `docs/ARCHITECTURE.md` and `docs/IMPLEMENTATION_PLAN.md`.

Intent→MCP adapter for repository status/PRs; most non-trivial GitHub operations are expected to go through `ai/claude` (Claude Code already has GitHub MCP access) rather than a separate direct client. Target milestone: MVP0.4.
