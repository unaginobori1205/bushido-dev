# core/planner

Status: **not implemented yet** — design only, see `docs/ARCHITECTURE.md` and `docs/IMPLEMENTATION_PLAN.md`.

Turns understanding into a suggested next action (PLAN, spec §2) instead of only answering — e.g. Morning Briefing recommendations, 'this task has been stalled 3 days' nudges (Proactive Mode, MVP0.5). Also owns the Daily→Long-term memory promotion filter ('is this worth remembering?'). Not part of MVP0.1.
