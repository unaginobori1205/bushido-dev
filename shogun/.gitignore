# secrets
.env
*.env.local

# dependencies / build output
node_modules/
dist/
build/
apps/desktop/src-tauri/target/
# regenerated by `cargo check`/`tauri dev`/`tauri build` — see
# https://v2.tauri.app/reference/acl/ (ACL schemas), not meant to be hand-edited or committed
apps/desktop/src-tauri/gen/

# local data (memory DB, action log — see docs/SECURITY.md)
database/*.sqlite
database/*.sqlite-*

# OS
.DS_Store
