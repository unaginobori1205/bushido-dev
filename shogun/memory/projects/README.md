# memory/projects

Status: **not implemented yet** — design only, see `docs/ARCHITECTURE.md` and `docs/IMPLEMENTATION_PLAN.md`.

Per-project Markdown files (`PROJECT.md`, `STATUS.md`, `NEXT_ACTIONS.md`, `DECISIONS.md`, `SESSION_LOG.md`) under `memory/projects/<slug>/`, matching spec §8's example — human-readable, diffable, and directly consumable by Claude Code when resuming a project. Target milestone: MVP0.3 ('continue yesterday's work').
